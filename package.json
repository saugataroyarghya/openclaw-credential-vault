{
  "name": "openclaw-credential-vault",
  "version": "0.1.0",
  "description": "Generic per-user credential vault plugin for OpenClaw — OAuth, API keys, MCP bridge, and auth gating.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/ index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "openclaw",
    "plugin",
    "oauth",
    "credential-vault",
    "mcp",
    "authentication"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "peerDependencies": {
    "openclaw": ">=0.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
